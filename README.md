# QuickItUp

To run the project:
1) Make sure you have node and npm installed.
2) Now clone the repo and in your terminal type "npm install" and click enter.
3) After that simply click on "Live Server" in your VS Code to see the result.
